import React from 'react'
import TopbarIconButton from '../TopbarIconButton'
import { VideoCall } from '@mui/icons-material'

const VideoIconButton = () => <TopbarIconButton icon={<VideoCall />} />

export default VideoIconButton
